[pytest]
minversion = 7.0
addopts = -ra -q --tb=short
testpaths =
    tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
norecursedirs = .git .github .vscode .venv venv build dist
filterwarnings =
    ignore::DeprecationWarning
